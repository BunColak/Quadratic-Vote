import test from "@playwright/test";

test.describe('create poll', () => {
    test('should allow adding new options', async ({browser}) => {})
    test('should allow removing options', async ({browser}) => {})
    test('should validate options', async ({browser}) => {})
    test('should submit form and redirect if successfull', async ({browser}) => {})
    test('should submit form and throw error if any', async ({browser}) => {})
})