import test from "@playwright/test";

test.describe('poll overview', () => {
    test('should show graph if there are any votes', async ({browser}) => {})
    test('should show no votes text if there is no votes yet', async ({browser}) => {})
    test('should show invite link', async ({browser}) => {})
    test('should copy invite link to clipboard on click', async ({browser}) => {})
    test('should validate if user is part of the poll', async ({browser}) => {})
    test('should show my poll page', async ({browser}) => {})
    test('should show edit button if user is the author', async ({browser}) => {})
})